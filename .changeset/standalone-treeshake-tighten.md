---
'@sanity/visual-editing-standalone': patch
---

Tighten standalone treeshake further (`propertyReadSideEffects` / `unknownGlobalSideEffects` / more pure factories) and use `minify: true` for the same full Oxc minify pass.
