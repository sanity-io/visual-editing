---
"@sanity/insert-menu": major
---

Deprecate `@sanity/insert-menu`. Its `InsertMenu` component has been inlined into `@sanity/visual-editing` (and `sanity`), so this package is no longer maintained and will not receive further updates.
