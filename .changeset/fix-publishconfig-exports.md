---
'@sanity/core-loader': patch
'@sanity/react-loader': patch
---

Add `publishConfig.exports` to improve package resolution for consumers by stripping the `source` export condition from published builds
