---
'@sanity/visual-editing': minor
'@sanity/visual-editing-standalone': major
---

Add a focused `enable-visual-editing` export and a self-contained ESM package
for non-React applications. The standalone package exposes
`enableVisualEditing` and `createDataAttribute` without runtime or peer
dependencies.
